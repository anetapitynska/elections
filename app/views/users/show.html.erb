<div id="page_wrapper">
  <p>
  <strong>Joined on:</strong>
  <%= @joined_on %>
</p>
<p>
  <strong>Last logged in on:</strong>
  <%= @last_login %>
</p>
<p>
  <strong>No. times logged in:</strong>
  <%= @user.sign_in_count %>
</p>
<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Role:</strong>
  <%= @user.role.name %>
</p>

<p>
  <strong>Area:</strong>
  <%= @user.area_id %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

  <% if @user.area_id.present? == true
   %> <p>
  <strong>Area:</strong> <%= @user_area.name %></p>
<p>
  <strong>Voivodeship:</strong> 
   <%= @user_voivodeship.name %> </p>
  <% end %>


<%= link_to 'Edit', edit_user_path(@user), class: "button"  %> 
<%= link_to 'Delete', @user, method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %></td>
<%= link_to 'Back', users_path, class: "button" %>
</div>